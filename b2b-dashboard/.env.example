# Copy to .env.local and fill in values

# Set to false to hit the real B2B API instead of mock data
VITE_MOCK=true

# Base URL for the B2B API (used when VITE_MOCK=false)
# In production this is proxied by Firebase Hosting → Cloud Run
VITE_API_BASE=/api
