{
  "name": "pom-functions",
  "version": "1.0.0",
  "description": "PoM Cloud Functions — Firestore triggers, credit logic, aggregations",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "jest --runInBand",
    "lint": "eslint ."
  },
  "dependencies": {
    "axios": "^1.7.2",
    "firebase-admin": "^12.3.0",
    "firebase-functions": "^5.1.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
